<%= form_for([@brand, @product, @shop]) do |f| %>
  <% if @shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop.errors.count, "error") %> prohibited this shop from being saved:</h2>

      <ul>
      <% @shop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shop_name %><br>
    <%= f.text_field :shop_name %>
  </div>
  <div class="field">
    <%= f.label :shop_image %><br>
    <%= f.text_field :shop_image %>
  </div>
  <div class="field">
    <%= f.label :shop_description %><br>
    <%= f.text_field :shop_description %>
  </div>
  <div class="field">
    <%= f.select  :id,  ShopView.all.map{ |c| [c.shop_view_name, c.id,{'data-shop_view_image': c.shop_view_image,'data-shop_view_name': c.shop_view_name}]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
